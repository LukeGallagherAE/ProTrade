# ProTrade Environment Variables

# Data directory (Railway Volume mount path)
# On Railway: set to /data
# Locally: leave unset (defaults to ./data inside project)
DATA_DIR=/data

# Node environment
NODE_ENV=production
